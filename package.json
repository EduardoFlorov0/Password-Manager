{
  "name": "secure-password-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Secure password manager with client-side encryption and security analysis.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "start": "npm run start --workspace server",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "vitest": "^4.1.9"
  }
}
